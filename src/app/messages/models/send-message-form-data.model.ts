export interface SendMessageFormData {
  title: string;
  firstName: string;
  lastName: string;
  primaryPhotoUri: string;
  memberId: string;
  message: string;
  fromMemberId: string;
  sendMessage: boolean;
}
